# AI短剧「代理编程（Multi-Agent）」落地方案

## 1. 目标定义（先统一“做成什么样”）

把“人盯流程”升级成“人提目标 + 多智能体自动协作执行”。

- 白天：人负责创意判断、方向决策、关键审片。
- 夜间：AI自动跑批，完成可标准化、可回放、可复盘的流程。
- 输出：第二天早上拿到可审阅的候选结果（分镜、提示词、素材、视频草版、成本报表）。

---

## 2. 核心架构：一个调度器 + 多个角色Agent

建议先做一个最小可用架构（MVP）：

### 2.1 Orchestrator（总控调度）
负责：
- 接收任务目标（例如：“把第3集剧本转成20条分镜并出图生视频提示词”）。
- 拆解任务为子任务。
- 分发给对应Agent。
- 记录状态（成功/失败/重试）。
- 汇总产物并生成日报。

### 2.2 角色Agent（建议首批6类）
1. **Script Analyst Agent（剧本解析）**
   - 输入：剧本文本。
   - 输出：场次、人物、情绪、冲突点、关键道具结构化信息。

2. **Storyboard Agent（分镜生成）**
   - 输入：结构化剧本信息。
   - 输出：镜号、景别、机位、时长、动作、台词、情绪标签。

3. **Prompt Engineer Agent（提示词工程）**
   - 输入：分镜脚本。
   - 输出：文生图提示词、图生视频提示词、负面提示词。
   - 附加：自动做提示词质量检查（长度、风格一致性、禁用词、可执行性）。

4. **Asset Generation Agent（素材生成）**
   - 输入：提示词包。
   - 输出：角色图、场景图、关键镜头图、视频片段。
   - 支持多模型回退（A模型失败自动切B模型）。

5. **QA & Review Agent（质量审查）**
   - 检查：人物一致性、场景连续性、镜头逻辑、字幕/台词匹配、违规风险。
   - 输出：通过/打回 + 具体修改建议。

6. **Cost Controller Agent（成本管控）**
   - 跟踪：每个任务/剧集/token/调用次数/模型单价。
   - 策略：超预算降级模型、降低采样数、延迟非关键任务。

### 2.3 技术实现选型（可直接开工）
- **任务编排**：Temporal / Prefect / Airflow（三选一，MVP建议 Prefect）
- **消息队列**：Redis + Celery（简单）或 Kafka（高并发）
- **数据库**：PostgreSQL（任务状态 + 成本 + 审计日志）
- **对象存储**：MinIO / OSS / S3（图片、视频、日报产物）
- **模型网关**：统一封装 `LLMClient` / `ImageClient` / `VideoClient`，支持多模型路由和回退
- **可观测性**：Prometheus + Grafana + Loki（成功率、重试率、成本、延迟）

### 2.4 最小数据模型（建议先建这4张表）
1. `projects`
   - `project_id`, `name`, `daily_budget`, `owner`, `status`
2. `jobs`
   - `job_id`, `project_id`, `episode_id`, `priority`, `status`, `scheduled_at`, `started_at`, `finished_at`
3. `tasks`
   - `task_id`, `job_id`, `agent_type`, `input_uri`, `output_uri`, `retry_count`, `status`, `error_code`
4. `usage_costs`
   - `task_id`, `model_name`, `input_tokens`, `output_tokens`, `unit_price`, `cost`, `created_at`

### 2.5 Agent调用协议（统一JSON，避免“接不上”）
所有Agent输入输出都走统一Envelope：

```json
{
  "trace_id": "job_2026_001_scene_03",
  "project_id": "proj_a01",
  "agent": "storyboard_agent",
  "input": {},
  "constraints": {
    "budget_limit": 30,
    "latency_limit_s": 120,
    "style_preset": "realistic_cinematic"
  }
}
```

输出建议统一包含：
- `status`: success/failed/review_required
- `artifacts`: 产物列表（URI + 类型）
- `metrics`: token、时延、成本、质量分
- `next_action`: 下一跳Agent或人工审核

### 2.6 Orchestrator执行逻辑（伪代码）

```python
def run_job(job):
    dag = [
      "script_analyst",
      "storyboard",
      "prompt_engineer",
      "asset_generation",
      "qa_review"
    ]

    for step in dag:
        if cost_guard_exceeded(job.project_id):
            downgrade_model_or_pause(job)

        result = dispatch(step, job)

        if result.status == "success":
            persist_artifacts(result)
            continue

        if result.status == "review_required":
            create_human_gate(job, step, result)
            break

        retry_or_fallback(job, step, result.error_code)

    finalize_daily_report(job)
```

### 2.7 失败重试与模型回退（必须规则化）
- **重试上限**：每个task最多2次；第3次失败直接转人工。
- **回退路径**：`gpt-4o -> gpt-4o-mini -> 规则模板兜底`。
- **错误分级**：
  - P1（平台不可用）：立即告警并暂停同类任务。
  - P2（提示词不合规）：自动改写后重试。
  - P3（质量不达标）：进入人工复审队列。

---

## 3. 夜间自动化流程（你们会议目标的直接落地）

可直接配置成定时任务（比如每天 23:00 启动）：

1. 自动拉取当天待处理任务（抽卡、分镜、提示词、出图、出视频）。
2. 按优先级入队（紧急项目优先）。
3. 多Agent并行执行。
4. 失败任务自动重试（建议最多2次）+ 错误分级告警。
5. 早上 9:00 输出：
   - 结果包（文件链接）
   - 质量评分
   - 成本报表
   - 待人工决策项

### 3.1 夜间调度时间表（建议默认配置）
- `22:50`：读取今日待处理列表并锁定预算。
- `23:00`：批量创建 `jobs`，按优先级入队。
- `23:00-06:30`：多Agent并发执行，失败自动重试+回退。
- `06:30-07:00`：QA Agent汇总质量评分，标记需人工复审项。
- `07:00-08:00`：Cost Agent生成成本报表并检查预算偏差。
- `08:30`：推送日报到飞书/企业微信。

### 3.2 并发策略（防止“把算力打爆”）
- 每项目最大并发：`max_concurrency_per_project = 5`
- 每模型QPS限流：由模型网关统一控制。
- 大任务拆片：视频任务按镜头切分为子任务，完成后合并。

### 3.3 日报模板（建议固定字段）
- 项目：`project_id` / 集数 / 任务总数 / 成功数 / 失败数
- 效率：平均单任务时长、P95时延
- 质量：通过率、打回率、Top3问题类型
- 成本：当日总成本、预算占比、单镜头平均成本
- 待办：需要人工决策的镜头清单（附产物链接）

### 3.4 可直接落地的最小脚本清单
1. `scheduler_nightly.py`：按时间创建并触发夜间批任务。
2. `worker_dispatcher.py`：把任务分发给对应Agent并回写状态。
3. `cost_guard.py`：预算检查、熔断、模型降级。
4. `report_builder.py`：生成日报Markdown并推送消息机器人。

---

## 4. 提示词优化体系（解决“成功率低、成本高”）

重点做一个 **Prompt Optimization Layer（提示词优化层）**，放在所有模型调用之前。

### 4.1 三层处理
1. **模板化**：
   - 把提示词拆为固定槽位：风格、镜头、人物、场景、动作、光影、质量约束。
2. **规则校验**：
   - 长度范围、冲突词检测、敏感词检测、格式规范。
3. **自动改写/压缩**：
   - 用小模型先优化提示词，再交给大模型/生图模型执行。

### 4.2 A/B评估机制
- 同一分镜并行生成2~3版提示词。
- 记录成功率、可用率、返工率、平均成本。
- 每周沉淀“高成功模板库”。

---

## 5. 成本管控体系（必须从第一天就接入）

### 5.1 成本看板最小字段
- 项目ID、剧集ID、任务类型
- 模型名称、调用次数、token消耗
- 成功率、重试率、平均单次成本
- 当日总成本、累计成本、预算偏差

### 5.2 三条硬规则（可直接执行）
1. **预算阈值熔断**：单项目日成本超阈值时，自动降级到低成本模型。
2. **失败率熔断**：某任务失败率超过设定值，停止无限重试并转人工。
3. **高价值优先**：优先保障“关键镜头”算力，边缘镜头降档生成。

---

## 6. 分阶段实施路线（建议6周）

### 第1-2周：MVP搭建
- 搭建任务队列 + Orchestrator。
- 跑通“剧本 -> 分镜 -> 提示词”链路。
- 接入基础日志与成本记录。

### 第3-4周：夜间无人值守
- 加入定时调度、重试机制、失败告警。
- 接入素材生成Agent（先图后视频）。
- 输出早报模板。

### 第5-6周：质量与成本优化
- 上线Prompt优化层。
- 上线A/B实验与模板库。
- 加入预算熔断与模型降级策略。

---

## 7. 团队分工建议（避免“全员都在改提示词”）

- **产品/制片**：定义任务目标、验收标准、预算上限。
- **编剧/导演**：审核创意与叙事质量，处理AI无法判断的艺术决策。
- **AI工程**：维护Agent流程、模型路由、质量规则、成本看板。
- **运营/PM**：跟踪日报、推动问题闭环、维护模板库。

---

## 8. 你可以今天就启动的“第一步清单”

1. 先选一个试点：例如“单集10个分镜自动化”。
2. 定义验收指标（建议4个）：
   - 成功率
   - 平均生成时长
   - 单镜头成本
   - 人工返工率
3. 用现有模型先手工串流程，记录每一步输入输出格式。
4. 把格式固化成JSON schema（后续Agent协同的关键）。
5. 再把流程接入夜间定时任务，跑一周拿数据。

---

## 9. 关键风险与规避

- **风险1：全自动但不可控**
  - 规避：所有任务保留人工“终审关口”。
- **风险2：模型效果波动**
  - 规避：多模型路由 + 回退策略 + 周度回归测试。
- **风险3：成本失控**
  - 规避：预算阈值 + 实时看板 + 熔断机制。

---

## 10. 一句话执行建议

先做“小范围夜间自动跑批 + 可量化指标 + 成本熔断”，
在2周内拿到第一批效率/成本数据，再逐步扩展到全流程。
